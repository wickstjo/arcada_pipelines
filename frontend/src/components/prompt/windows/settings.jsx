function Settings({ prompt_state }) {
    return (
        <div id={ 'innerbody' }>
            <div id={ 'title' }>Settings</div>
            <div id={ 'backdrop' }>
                <div className={ 'content' }>
                    Settings Prompt
                </div>
            </div>
        </div>
    )
}

export default Settings