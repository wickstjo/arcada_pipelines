experiment:
    name: str
    version: int

dataset:
    db_table: str
    stock_symbol: str
    timestamps:
        # start: lambda x: type(x) == str and len(x)==19
        start: str
        end: str
    min_row_count: int
